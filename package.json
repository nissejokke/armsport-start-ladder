{
  "name": "armsport-ladder",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "build": "bun build --minify src/main.ts src/store.ts --outdir src/dist",
    "serve": "serve src",
    "dev": "bun build src/main.ts src/store.ts --outdir src/dist --watch",
    "test": "deno test"
  },
  "author": "Johan",
  "license": "ISC",
  "devDependencies": {
    "bun": "^0.6.12",
    "serve": "^14.2.0"
  }
}
